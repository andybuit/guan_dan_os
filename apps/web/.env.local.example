# WebSocket Configuration
# Leave empty to disable WebSocket connections (useful during UI development)
# Set to your WebSocket server URL when backend is ready
# Example: NEXT_PUBLIC_WS_URL=ws://localhost:3001
NEXT_PUBLIC_WS_URL=
